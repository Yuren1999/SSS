# 吸收光谱数据模拟软件开发流程规划

## 1. 规划目标

本文档基于《吸收光谱数据模拟软件设计文档》制定开发流程。开发策略采用模块化、分阶段、接口先行的方式，目标是在逐步实现桌面 GUI、HITRAN/HAPI 谱线下载、本地气体谱线数据库、本地检索和光谱批量合成时，尽量避免某一模块修改牵连其他模块。

核心原则：

1. 模块职责清晰，避免跨层直接调用内部实现。
2. 先定义数据结构和接口，再实现业务逻辑。
3. GUI 只调用应用服务，不直接访问数据库、HAPI 或计算函数。
4. 下载、本地数据库、本地检索、光谱合成、批处理和导出互相解耦。
5. 每个阶段完成后保留可运行、可测试的小版本。

## 2. 总体开发阶段

| 阶段 | 名称 | 核心目标 | 主要产物 |
| --- | --- | --- | --- |
| P0 | 工程骨架 | 建立项目结构、依赖、配置、测试框架 | 可运行空 GUI、基础目录、测试入口 |
| P1 | 数据模型与接口 | 固定核心数据结构和模块接口 | dataclass/schema、服务接口、错误类型 |
| P2 | 本地谱线数据库 | 实现本地数据库 schema、覆盖范围管理和本地检索 | SQLite/Parquet 数据库、Repository、覆盖检查 |
| P3 | HAPI 下载 | 实现 HITRAN/HAPI 下载并写入本地数据库 | 下载服务、任务日志、增量下载 |
| P4 | 单气体合成 | 基于本地数据库实现单气体 line-by-line 合成 | 波数网格、谱线检索、吸收谱计算 |
| P5 | 混合气体与扰动 | 实现 H2O/CO2 常驻背景、变量组分、噪声、基线 | 混合谱、标签、扰动分量 |
| P6 | 批量合成与导出 | 实现批量参数展开、任务队列和数据集导出 | HDF5/Parquet/NPZ、日志、失败样本 |
| P7 | GUI 完整化 | 将各服务接入桌面 GUI，完善交互和结果浏览 | 完整桌面软件原型 |
| P8 | 打包与验收 | 完成测试、文档、打包和示例数据 | 可交付安装包、用户说明、测试报告 |

## 3. 模块边界设计

### 3.1 模块划分

```text
spectra_sim/
  app/             桌面 GUI，只负责交互和状态展示
  services/        应用服务层，连接 GUI 与底层模块
  hitran/          HAPI 下载适配器
  database/        本地谱线数据库与 Repository
  synthesis/       光谱合成计算核心
  batch/           批量参数展开和任务运行
  io/              数据导出、元数据写入
  validation/      参数校验和业务规则
  models/          共享数据结构和枚举
  tests/           单元测试与集成测试
```

### 3.2 依赖方向

```text
app
  -> services
      -> hitran
      -> database
      -> synthesis
      -> batch
      -> io
      -> validation
      -> models

底层模块只允许依赖 models、validation 或标准科学计算库。
```

禁止依赖方向：

- `synthesis` 不得依赖 `app`、`hitran` 或 GUI 控件。
- `database` 不得依赖 `app` 或 `synthesis`。
- `hitran` 不得依赖 `synthesis`。
- `io` 不得反向调用 GUI。
- GUI 页面不得直接调用 SQLite、HAPI 或 NumPy 计算细节。

## 4. 核心接口先行

### 4.1 共享数据模型

优先定义以下模型，并尽量保持稳定：

```text
GasSpec
  gas_name
  hitran_molecule_id

WavenumberRange
  nu_min
  nu_max

LineRecord
  gas_name
  wavenumber
  line_intensity
  air_width
  self_width
  lower_state_energy
  temperature_dependence
  pressure_shift

LineTable
  gas_name
  range
  records

SynthesisConfig
  spectral_axis
  environment
  resident_gases
  variable_gases
  noise
  baseline
  output

SpectrumRecord
  sample_id
  wavenumber
  clean_absorbance
  baseline
  noise
  final_absorbance
  transmittance
  labels
  metadata
```

### 4.2 应用服务接口

GUI 只调用服务接口：

```text
LineDownloadService
  create_download_task(...)
  run_download_task(...)
  list_download_tasks(...)

LineDatabaseService
  list_gases()
  get_coverage(gas_name)
  check_coverage(gases, nu_range)
  search_lines(gas_name, nu_range)

SynthesisService
  synthesize_single(config)
  synthesize_mixture(config)
  preview(config)

BatchSynthesisService
  expand_parameters(batch_config)
  run_batch(batch_config)
  pause_task(task_id)
  resume_task(task_id)

ExportService
  export_spectra(records, output_config)
```

接口一旦被 GUI 使用，修改时必须优先保持兼容。确需变更时，应新增参数或新增方法，避免直接改旧方法语义。

## 5. 分阶段开发流程

### P0 工程骨架

目标：

- 创建 Python 包结构。
- 确定 GUI 框架，建议 PySide6。
- 建立配置文件、日志系统和测试框架。
- 提供一个可启动的空主窗口。

建议只修改：

- `pyproject.toml`
- `src/spectra_sim/app/`
- `src/spectra_sim/models/`
- `tests/`

验收标准：

- 软件能启动空 GUI。
- 测试命令能运行。
- 日志能写入本地文件。

### P1 数据模型与接口

目标：

- 定义核心数据模型。
- 定义统一异常类型。
- 定义服务接口或抽象基类。
- 完成基础参数校验。

建议只修改：

- `models/`
- `validation/`
- `services/interfaces.py`

验收标准：

- 数据模型单元测试通过。
- 参数非法时能返回明确错误。
- 不接入 HAPI、不接入数据库、不实现 GUI 业务。

### P2 本地谱线数据库

目标：

- 实现本地气体谱线数据库 schema。
- 实现气体表、谱线表、覆盖范围表、下载任务表。
- 实现覆盖范围合并、缺失区间检测、本地谱线检索。

建议只修改：

- `database/`
- `models/`
- `validation/`
- 对应测试文件

模块边界：

- 数据库模块只负责存储和检索。
- 不负责 HAPI 下载。
- 不负责吸收谱计算。

验收标准：

- 可插入模拟谱线数据。
- 可查询某气体覆盖范围。
- 可判断目标波数范围是否完整覆盖。
- 可返回缺失区间。

### P3 HAPI 下载

目标：

- 封装 HAPI 下载逻辑。
- 将下载结果转换为内部 `LineRecord`。
- 调用数据库 Repository 写入本地数据库。
- 支持新增下载、跳过已覆盖范围和覆盖已有范围。

建议只修改：

- `hitran/`
- `services/download_service.py`
- 少量 `database/` Repository 调用测试

模块边界：

- HAPI 模块只负责远程下载和字段转换。
- 数据库写入通过 Repository 完成，不直接拼 SQL。
- 不调用合成模块。

验收标准：

- 可下载指定气体、指定波数范围数据。
- 下载完成后本地数据库覆盖范围更新。
- 网络失败能记录错误并允许重试。

### P4 单气体合成

目标：

- 实现波数网格生成。
- 实现本地谱线读取。
- 实现单气体 line-by-line 吸收谱计算。
- 输出 clean absorbance 和 transmittance。

建议只修改：

- `synthesis/grid.py`
- `synthesis/line_shape.py`
- `synthesis/absorbance.py`
- `services/synthesis_service.py`
- 对应测试文件

模块边界：

- 合成模块不允许联网下载谱线。
- 合成模块通过 `LineDatabaseService` 或 Repository 获取本地谱线。
- 缺失谱线数据时返回覆盖不足错误，不自动补下载。

验收标准：

- 浓度或光程增大时吸光度增大。
- 相同输入参数输出一致。
- 无本地覆盖时能明确报错。

### P5 混合气体与扰动模型

目标：

- 实现 H2O/CO2 常驻背景与三类变量组分混合合成。
- 实现存在性标签和浓度标签。
- 实现 Gaussian 噪声、多项式基线、随机平滑基线。

建议只修改：

- `synthesis/mixture.py`
- `synthesis/noise.py`
- `synthesis/baseline.py`
- `models/labels.py`

模块边界：

- 噪声、基线模块不直接访问数据库。
- 标签生成逻辑不写在 GUI 页面中。
- 混合气体合成只组合单气体合成结果，不重复实现谱线计算。

验收标准：

- 混合谱等于单气体吸光度叠加。
- 变量组分不存在时 presence 为 0，浓度为 0。
- 同一随机种子下噪声和基线可复现。

### P6 批量合成与导出

目标：

- 实现参数空间展开。
- 实现网格扫描、随机采样、外部参数表导入。
- 实现批量任务进度、失败样本记录。
- 实现 HDF5、Parquet、NPZ 导出。

建议只修改：

- `batch/`
- `io/`
- `services/batch_synthesis_service.py`
- 对应测试文件

模块边界：

- `batch` 只负责组织任务，不实现具体吸收谱计算。
- `io` 只负责写文件，不生成物理数据。
- 导出格式变化不应影响合成核心。

验收标准：

- 能生成多样本数据集。
- 输出包含光谱数组、标签、参数表和元数据。
- 单个样本失败不导致整个任务丢失进度。

### P7 GUI 完整化

目标：

- 接入数据库页面、下载页面、检索页面、单条合成页面、批量合成页面和结果浏览页面。
- 所有长任务放入后台 worker。
- 绘图支持 clean、baseline、noise、final absorbance 开关显示。

建议只修改：

- `app/`
- `services/` 的薄封装

模块边界：

- GUI 不写 SQL。
- GUI 不直接调用 HAPI。
- GUI 不直接实现物理计算公式。
- GUI 只负责收集参数、调用服务、展示结果。

验收标准：

- 下载任务运行时界面不卡死。
- 合成前能检查本地覆盖。
- 覆盖不足时能显示缺失气体和缺失波数区间。
- 结果预览曲线正常。

### P8 打包与验收

目标：

- 完成桌面程序打包。
- 固化示例配置和示例数据库。
- 输出用户手册和测试报告。

建议只修改：

- `packaging/`
- `docs/`
- `configs/templates/`

验收标准：

- 新环境中可安装或解压运行。
- 示例下载、检索、合成、导出流程可跑通。
- 关键单元测试和集成测试通过。

## 6. 防止过度改动的开发规则

### 6.1 每个任务先声明修改范围

开始编码前明确：

```text
本任务允许修改：
- database/repository.py
- database/coverage.py
- tests/test_coverage.py

本任务不修改：
- synthesis/
- app/
- hitran/
```

如果实现过程中发现必须越界修改，应先记录原因，再决定是否拆出新任务。

### 6.2 公共模型保持稳定

`models/` 是全局共享层。修改公共模型时影响面最大，应遵循：

- 优先新增字段，不删除字段。
- 优先新增模型，不改变旧模型语义。
- 必须有迁移说明和测试。
- GUI 已使用的字段不能随意改名。

### 6.3 服务层作为隔离边界

GUI 与底层模块之间必须通过 `services/` 隔离。这样未来替换数据库、替换 HAPI 适配器或优化合成算法时，GUI 不需要大规模改动。

### 6.4 计算核心纯函数化

`synthesis/` 中的核心函数应尽量保持纯函数风格：

- 输入为数组和参数。
- 输出为数组和结构化结果。
- 不读写数据库。
- 不读写 GUI 状态。
- 不直接写文件。

### 6.5 数据库访问集中在 Repository

所有 SQL、文件索引、覆盖范围查询都集中在 `database/`。其他模块不得直接拼 SQL 或直接操作数据库连接。

### 6.6 长任务统一走 worker

下载、批量合成和大文件导出均属于长任务，应统一走后台 worker 或任务队列。GUI 页面只订阅进度事件，不直接执行耗时逻辑。

### 6.7 测试跟随模块提交

每个模块提交至少包含对应测试：

- `database` 修改必须有覆盖范围和检索测试。
- `hitran` 修改必须有字段转换和失败处理测试。
- `synthesis` 修改必须有物理一致性测试。
- `batch` 修改必须有参数展开和失败恢复测试。
- `io` 修改必须有导出后重读测试。

## 7. 推荐开发顺序

第一轮只做最小闭环：

1. 空 GUI 启动。
2. 本地数据库创建。
3. 插入模拟谱线数据。
4. 本地检索覆盖检查。
5. 单气体吸收谱合成。
6. GUI 中预览一条 clean absorbance。

第二轮接入真实下载：

1. HAPI 下载。
2. 下载写入数据库。
3. GUI 显示覆盖范围。
4. 用真实本地谱线合成单气体光谱。

第三轮扩展训练数据能力：

1. H2O/CO2 常驻组分。
2. 三类变量组分。
3. 噪声、基线。
4. 标签生成。
5. 批量导出。

第四轮完善桌面软件体验：

1. 后台任务队列。
2. 任务日志。
3. 结果浏览。
4. 数据库完整性检查。
5. 打包发布。

## 8. 最小可运行版本定义

建议将第一个可运行版本定义为：

1. 桌面 GUI 能启动。
2. 能创建本地谱线数据库。
3. 能导入或插入一份模拟谱线数据。
4. 能在本地检索页面检查谱线覆盖。
5. 能在单条合成页面生成并预览一条吸收谱。
6. 合成过程中不联网下载。

这个版本不追求完整 HITRAN 下载和批量生成，但可以尽早验证模块边界、GUI 调用链和本地数据库设计是否合理。

## 9. 每个阶段的完成定义

一个阶段完成前必须满足：

1. 功能能通过 GUI 或测试入口跑通。
2. 新增或修改模块有对应测试。
3. 未越界修改无关模块。
4. 文档中的接口说明与代码一致。
5. 错误信息可读，不直接暴露底层堆栈给用户。
6. 关键流程有日志。

## 10. 后续实施建议

下一步建议从 P0 和 P1 开始：

1. 创建 Python 工程骨架。
2. 确认 GUI 采用 PySide6。
3. 定义 `models/` 中的核心数据结构。
4. 定义 `services/` 接口。
5. 建立测试框架。

完成这些基础工作后，再进入本地谱线数据库开发。这样可以先把模块边界固定下来，后续实现下载、合成和批量导出时不会频繁改动 GUI 或公共数据结构。
