# 吸收光谱数据模拟软件设计文档

## 1. 文档目标

本文档用于定义吸收光谱数据模拟软件的产品边界、需求设计、功能设计、数据架构和阶段实施路线。软件面向开放光路宽光谱多组分识别算法研究，核心用途是基于 HITRAN 谱线数据批量合成带标签的吸收光谱数据，为后续多组分定量识别、存在性判别、物理重构验证和算法训练提供可控数据来源。

软件核心功能分为两条相互独立的主线：

1. 谱线数据下载与本地数据库建设：通过 HAPI 从 HITRAN 下载指定气体、指定波数范围内的 line-by-line 谱线数据，并写入本地气体谱线数据库。
2. 本地谱线检索与光谱批量合成：在已下载的本地气体数据库基础上，按照气体种类、浓度、光程、压力、温度、波数范围、采样间隔、噪声和基线等参数批量生成吸收光谱数据。

## 2. 项目背景与软件定位

根据项目方案，整体研究任务聚焦开放光路宽光谱场景下的多组分吸收信号联合反演。算法侧关注 H2O/CO2 常驻背景项与三类变量待检组分在强背景、弱目标、吸收峰重叠条件下的智能解耦识别。训练数据需要覆盖不同环境条件、组分组合、浓度范围、背景扰动和困难样本边界。

本软件在项目中的定位不是通用光谱数据库浏览器，而是面向算法训练和验证的数据生成工具。它需要稳定生成以下类型的数据：

- H2O/CO2 常驻背景组分与三类变量目标组分共同作用下的混合吸收谱。
- 同一波数网格下的吸光度谱、透过率谱及其物理分量。
- 带有浓度标签、存在性标签、环境参数、基线项和噪声项的监督训练样本。
- 可用于物理重构验证的 clean spectrum、baseline、noise、final spectrum 分解结果。

## 3. 设计边界与约束

### 3.1 本版明确采用的边界

| 项目 | 设计约束 |
| --- | --- |
| 软件形态 | 仅开发桌面级 GUI 软件，不规划 Web 端和命令行产品入口 |
| 光谱坐标 | 所有光谱相关功能统一采用波数，单位为 cm^-1 |
| 谱线来源 | 通过 HAPI 从 HITRAN 获取 line-by-line 谱线数据 |
| 同位素 | 不提供同位素选择、同位素筛选和同位素独立下载功能 |
| 数据流程 | 下载与本地检索相互独立，合成阶段只使用本地已下载数据 |
| 仪器线型 | 不考虑仪器线型卷积、仪器响应函数和分辨率退化模型 |
| 第一版重点 | 本地谱线数据库、波数域光谱合成、批量样本生成、数据导出 |

### 3.2 同位素处理说明

用户层面不暴露同位素参数，也不要求按同位素下载。若 HITRAN 原始谱线记录中包含同位素字段，软件可以在底层数据库中保留该字段用于追溯，但不作为界面筛选条件、下载条件或合成参数。

### 3.3 波数单位说明

所有涉及光谱横轴的参数均使用波数 `cm^-1`：

- 下载范围：`nu_min`、`nu_max`
- 本地检索范围：`nu_min`、`nu_max`
- 合成范围：`nu_start`、`nu_end`
- 采样间隔：`nu_step`
- 输出横轴：`wavenumber`

软件不提供波长输入、波长输出、波长/波数转换或按波长等间隔采样功能。

### 3.4 关键代码中文注释要求

本项目面向科研软件开发，后续需要长期维护、调试和验证物理计算过程。关键代码必须使用中文注释说明设计意图、物理含义和边界条件，但不要求对每一行简单赋值或显而易见的语句进行注释。

#### 必须添加中文注释的代码范围

| 代码位置 | 中文注释重点 |
| --- | --- |
| HITRAN/HAPI 下载适配 | 说明气体编号映射、下载范围、字段转换和异常处理逻辑 |
| 本地数据库覆盖范围判断 | 说明覆盖区间合并、缺失区间检测和重复下载规避逻辑 |
| 谱线字段转换 | 说明 HITRAN 原始字段与内部字段之间的对应关系 |
| 波数网格生成 | 说明 `nu_start`、`nu_end`、`nu_step` 的取值规则和点数计算方式 |
| 温度/压力修正 | 说明谱线强度修正、展宽修正和单位处理逻辑 |
| line-by-line 吸收计算 | 说明线型函数、谱线叠加、吸收系数计算的物理意义 |
| Beer-Lambert 计算 | 说明吸光度、透过率、光程和吸收系数之间的关系 |
| 混合气体合成 | 说明各组分吸收贡献如何叠加，以及常驻组分和变量组分的处理差异 |
| 噪声与基线模型 | 说明随机种子、扰动幅值、基线函数和输出分量的含义 |
| 批量参数展开 | 说明网格扫描、随机采样、分层采样和外部参数表解析逻辑 |
| 数据导出 | 说明光谱数组、标签、元数据和随机种子的保存结构 |
| GUI 后台任务 | 说明线程/worker 与主界面的通信、进度更新和异常回传方式 |

#### 注释粒度要求

1. 对复杂函数，在函数开头用中文说明函数目的、输入输出和关键假设。
2. 对物理公式，在公式实现前用中文说明公式含义和变量单位。
3. 对容易出错的边界条件，在判断逻辑前说明为什么需要该判断。
4. 对数据库区间合并、缺失检测、批量采样等非直观流程，应添加步骤级中文注释。
5. 对简单赋值、常规循环、明显的 UI 控件绑定，不需要机械性注释。

#### 注释示例

```python
def build_wavenumber_grid(nu_start: float, nu_end: float, nu_step: float) -> np.ndarray:
    """生成升序波数网格，所有光谱计算均在 cm^-1 单位下进行。"""
    # 波数范围必须为正，避免生成无物理意义的光谱坐标
    if nu_start <= 0 or nu_end <= 0:
        raise ValueError("波数范围必须为正数")

    # 统一按升序生成网格，避免后续谱线插值和数组叠加时出现方向不一致
    start, end = sorted((nu_start, nu_end))
    return np.arange(start, end + nu_step * 0.5, nu_step)
```

```python
def apply_beer_lambert(absorption_coefficient: np.ndarray, path_length: float) -> tuple[np.ndarray, np.ndarray]:
    """根据 Beer-Lambert 定律计算吸光度和透过率。"""
    # A(nu) = alpha(nu) * L，alpha 为吸收系数，L 为吸收光程
    absorbance = absorption_coefficient * path_length

    # T(nu) = exp(-A)，透过率用于后续与实测透射谱进行对照
    transmittance = np.exp(-absorbance)
    return absorbance, transmittance
```

## 4. 需求设计

### 4.1 用户角色

| 角色 | 主要诉求 |
| --- | --- |
| 光谱算法研究人员 | 构建覆盖不同组分、浓度、温压、噪声和基线条件的训练数据集 |
| 实验与测试人员 | 根据开放光路测量条件模拟吸收谱，并与实测谱进行对照 |
| 数据集维护人员 | 批量下载和扩展本地气体谱线数据库，检查谱线覆盖范围 |
| 软件开发人员 | 扩展合成模型、批处理策略、导出格式和 GUI 交互 |

### 4.2 功能性需求

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| R1 | 支持在桌面 GUI 中配置并批量下载多个气体的 HITRAN 谱线数据 | 高 |
| R2 | 支持将下载结果写入本地气体谱线数据库，并记录气体、波数覆盖范围和下载时间 | 高 |
| R3 | 支持查看本地数据库已有气体、波数覆盖范围、谱线数量和数据状态 | 高 |
| R4 | 支持独立的本地谱线检索功能，按气体和波数范围从本地数据库读取谱线 | 高 |
| R5 | 光谱合成阶段只使用本地数据库，缺失数据时提示用户先下载，不自动联网下载 | 高 |
| R6 | 支持输入气体种类、浓度、光程、压力、温度、波数范围、采样间隔等参数生成吸收谱 | 高 |
| R7 | 支持 H2O/CO2 常驻背景组分与三类变量待检组分的混合气体合成，并允许后续扩展变量组分数量 | 高 |
| R8 | 支持批量参数组合生成，包括网格扫描、随机采样和外部参数表导入 | 高 |
| R9 | 支持添加高斯噪声、随机噪声、尖峰扰动、多项式基线和低频基线漂移 | 高 |
| R10 | 支持保存 clean spectrum、baseline、noise、final spectrum 及完整标签 | 高 |
| R11 | 支持导出 CSV、NPZ、HDF5、Parquet 等数据格式 | 中 |
| R12 | 支持光谱预览、批量任务进度显示、日志记录和失败样本追踪 | 中 |

### 4.3 非功能性需求

| 类别 | 要求 |
| --- | --- |
| 准确性 | 谱线强度、温度修正、压力展宽、吸收系数和 Beer-Lambert 计算必须可测试 |
| 可复现性 | 每次合成任务记录参数、随机种子、谱线数据库版本、软件版本和导出时间 |
| 扩展性 | 本地气体数据库支持后续增量下载更多气体和更多波数范围 |
| 性能 | 支持万级样本批量生成，大范围波数计算采用分块和缓存策略 |
| 易用性 | GUI 参数需要有默认模板、范围校验、单位固定提示和错误说明 |
| 稳定性 | 网络失败、数据库缺失、谱线覆盖不足、参数非法时给出明确提示 |

## 5. 总体架构设计

### 5.1 分层架构

```text
桌面 GUI 层
  - 数据库管理页面
  - 本地谱线检索页面
  - 单条光谱合成页面
  - 批量合成任务页面
  - 结果预览与导出页面

应用服务层
  - HITRAN/HAPI 下载服务
  - 本地谱线数据库服务
  - 光谱合成服务
  - 批处理任务服务
  - 数据导出服务

计算核心层
  - 波数网格生成
  - 谱线筛选与温压修正
  - line-by-line 吸收截面/吸收系数计算
  - Beer-Lambert 吸收谱合成
  - 混合气体叠加
  - 噪声与基线模型

数据层
  - 本地气体谱线数据库
  - 下载任务记录
  - 合成任务配置
  - 合成光谱数据
  - 元数据与日志
```

### 5.2 推荐技术栈

| 模块 | 建议 |
| --- | --- |
| GUI | PySide6 或 PyQt6 |
| 绘图 | PyQtGraph，必要时配合 Matplotlib |
| 科学计算 | NumPy、SciPy |
| 数据处理 | Pandas、PyArrow |
| 本地数据库 | SQLite 管理元数据与索引，谱线表可存 SQLite 或 Parquet |
| 批量结果 | HDF5、Parquet、NPZ |
| 打包 | PyInstaller 或 Nuitka |

### 5.3 推荐目录结构

```text
spectra-simulator/
  src/
    spectra_sim/
      app/
        main_window.py
        pages/
          database_page.py
          search_page.py
          synthesis_page.py
          batch_page.py
          result_page.py
      hitran/
        hapi_downloader.py
        molecule_registry.py
      database/
        schema.py
        repository.py
        coverage.py
        integrity.py
      synthesis/
        grid.py
        line_shape.py
        absorbance.py
        mixture.py
        noise.py
        baseline.py
      batch/
        parameter_space.py
        runner.py
        worker.py
      io/
        exporters.py
        metadata.py
      validation/
        schema.py
        rules.py
  configs/
    templates/
  data/
    local_line_db/
    outputs/
  tests/
```

## 6. 核心功能设计

### 6.1 谱线数据下载模块

#### 功能定位

该模块只负责从 HITRAN/HAPI 下载数据并写入本地气体谱线数据库，不直接参与光谱合成。用户可以在正式生成训练数据前，先批量下载项目所需的气体谱线范围。

#### 输入参数

| 参数 | 说明 |
| --- | --- |
| gas_name | 气体名称，例如 H2O、CO2、CH4 |
| hitran_molecule_id | HITRAN 分子编号，可由软件内置表自动映射 |
| nu_min | 下载起始波数，单位 cm^-1 |
| nu_max | 下载终止波数，单位 cm^-1 |
| download_mode | 新增下载、覆盖已有范围、跳过已覆盖范围 |
| task_name | 下载任务名称，便于追踪 |

#### 功能流程

1. 用户在 GUI 中添加一个或多个下载任务。
2. 软件校验气体名称、HITRAN 编号和波数范围。
3. 软件查询本地数据库已有覆盖范围。
4. 根据下载模式判断需要新增下载的波数区间。
5. 调用 HAPI 下载对应气体和波数范围内的谱线数据。
6. 将谱线数据转换为内部标准字段并写入本地数据库。
7. 更新气体覆盖范围、谱线数量、下载时间和任务日志。

#### 设计要点

- 下载任务支持队列化，避免 GUI 阻塞。
- 下载失败时保留失败原因，并允许重试。
- 后续扩展下载时，应能追加新气体或扩展已有气体的波数范围。
- 已有区间和新增区间需要合并管理，避免重复下载造成数据库冗余。

### 6.2 本地气体谱线数据库模块

#### 功能定位

本地数据库是下载模块与合成模块之间的隔离层。合成模块只访问本地数据库，不直接调用 HAPI。

#### 数据库核心能力

| 能力 | 说明 |
| --- | --- |
| 气体管理 | 记录已下载气体名称、HITRAN 编号、谱线数量 |
| 覆盖范围管理 | 记录每种气体已覆盖的波数区间，并支持区间合并 |
| 谱线检索 | 按气体和波数范围读取 line-by-line 谱线 |
| 数据扩展 | 支持后续继续下载新气体或扩大已有气体波数范围 |
| 完整性检查 | 检查谱线表、覆盖表、任务记录是否一致 |
| 版本追踪 | 记录下载时间、数据来源、软件版本和数据库 schema 版本 |

#### 建议数据表

```text
gas_catalog
  gas_id
  gas_name
  hitran_molecule_id
  description
  created_at
  updated_at

line_coverage
  coverage_id
  gas_id
  nu_min
  nu_max
  line_count
  source
  downloaded_at

spectral_lines
  line_id
  gas_id
  wavenumber
  line_intensity
  air_width
  self_width
  lower_state_energy
  temperature_dependence
  pressure_shift
  raw_fields

download_tasks
  task_id
  task_name
  gas_id
  nu_min
  nu_max
  status
  message
  started_at
  finished_at
```

#### 检索规则

1. 检索输入只包含气体名称和波数范围。
2. 检索前先检查数据库覆盖范围是否完整覆盖目标范围。
3. 如果覆盖不足，返回缺失区间并提示用户先进入下载页面补充数据。
4. 检索结果返回统一的 `LineTable`，供合成模块使用。

### 6.3 波数网格生成模块

#### 输入参数

| 参数 | 说明 |
| --- | --- |
| nu_start | 起始波数，单位 cm^-1 |
| nu_end | 终止波数，单位 cm^-1 |
| nu_step | 采样间隔，单位 cm^-1 |

#### 输出

- `wavenumber_grid`
- 网格点数
- 起止波数
- 采样间隔

#### 设计要点

- 允许 `nu_start < nu_end`，输出升序波数网格。
- 第一版不支持波长坐标。
- `nu_step` 表示模拟输出的采样间隔，不等同于仪器线型或仪器分辨率。

### 6.4 单气体吸收谱合成模块

#### 输入参数

| 参数 | 说明 |
| --- | --- |
| gas_name | 气体名称 |
| concentration | 浓度，建议支持 ppm、ppb、体积分数 |
| path_length | 吸收光程 |
| pressure | 测量压力 |
| temperature | 环境温度 |
| nu_start / nu_end | 合成波数范围，单位 cm^-1 |
| nu_step | 采样间隔，单位 cm^-1 |
| line_shape | 线型模型，默认 Voigt |

#### 计算流程

1. 根据气体和波数范围从本地数据库检索谱线。
2. 生成统一波数网格。
3. 根据温度、压力和浓度修正谱线强度与展宽参数。
4. 使用 line-by-line 模型计算吸收系数。
5. 根据 Beer-Lambert 定律计算吸光度和透过率。
6. 返回 clean absorbance 和 transmittance。

#### 基本模型

```text
T(nu) = exp(-alpha(nu) * L)
A(nu) = -ln(T(nu))
```

其中：

- `nu` 为波数。
- `alpha(nu)` 为吸收系数。
- `L` 为吸收光程。

### 6.5 混合气体合成模块

#### 功能说明

支持 H2O/CO2 常驻背景组分与三类变量待检组分的混合吸收谱合成，并允许后续扩展变量组分数量。每种气体拥有独立浓度，所有组分共享同一波数网格、压力、温度和光程。

#### 合成逻辑

```text
alpha_total(nu) = sum(alpha_i(nu))
A_total(nu) = sum(A_i(nu))
T_total(nu) = exp(-A_total(nu))
```

#### 标签设计

| 标签 | 说明 |
| --- | --- |
| resident_concentration | 常驻组分浓度，例如 H2O、CO2 |
| variable_presence | 变量组分是否存在，0 或 1 |
| variable_concentration | 变量组分浓度，不存在时为 0 |
| environment | 温度、压力、光程 |
| baseline_params | 基线模型及参数 |
| noise_params | 噪声模型及参数 |

#### 面向算法训练的输出

- 原始吸光度谱 `absorbance`
- 存在性标签 `presence_labels`
- 浓度标签 `concentration_labels`
- 物理重构所需的各组分 clean contribution

### 6.6 批量合成模块

#### 参数生成方式

| 方式 | 说明 |
| --- | --- |
| 网格扫描 | 对多个参数取笛卡尔积，生成全组合 |
| 随机采样 | 在给定范围内随机采样 |
| 分层采样 | 针对常驻组分、变量组分和困难样本分别设置采样规则 |
| 外部参数表 | 从 CSV、Excel、YAML 或 JSON 读取参数组合 |

#### 批处理流程

1. 用户在 GUI 中选择批量合成模板。
2. 设置常驻组分、变量组分、浓度范围、温度、压力、光程、波数范围和采样间隔。
3. 软件检查所有气体在本地数据库中的谱线覆盖范围。
4. 如果覆盖不足，列出缺失气体和缺失波数区间。
5. 覆盖完整后，展开参数空间并生成样本任务。
6. 分块计算 clean spectrum。
7. 添加基线和噪声，生成 final spectrum。
8. 保存光谱数组、标签、参数表和日志。

#### 输出组织方式

```text
outputs/
  task_2026xxxx_xxxxxx/
    task_config.yaml
    expanded_parameters.parquet
    spectra.h5
    labels.parquet
    metadata.json
    preview/
      sample_0001.png
    logs/
      task.log
```

### 6.7 噪声模型模块

#### 第一版支持

| 噪声类型 | 参数 |
| --- | --- |
| 高斯白噪声 | 标准差、信噪比 |
| 均匀噪声 | 幅值范围 |
| 相对强度噪声 | 噪声比例 |
| 随机尖峰 | 尖峰数量、幅度范围、宽度 |

#### 设计要求

- 所有随机过程必须支持随机种子。
- 噪声可以作用于吸光度谱。
- 输出时应保留 `noise` 数组，便于监督训练和误差分析。

### 6.8 基线模型模块

#### 第一版支持

| 基线类型 | 参数 |
| --- | --- |
| 常数偏移 | offset |
| 线性漂移 | slope、intercept |
| 多项式基线 | polynomial order、coefficients |
| 正弦起伏 | amplitude、period、phase |
| 随机平滑基线 | 控制点数量、幅度、平滑系数 |

#### 设计要点

- 基线作为独立数组叠加到 clean absorbance 上。
- 输出时保留 `clean_absorbance`、`baseline`、`noise`、`final_absorbance`。
- 基线模型用于模拟开放光路背景扰动，不涉及仪器线型卷积。

## 7. 数据模型设计

### 7.1 谱线数据结构

```text
LineTable
  gas_id
  gas_name
  hitran_molecule_id
  wavenumber
  line_intensity
  air_width
  self_width
  lower_state_energy
  temperature_dependence
  pressure_shift
  source
  downloaded_at
```

### 7.2 合成配置结构

```yaml
spectral_axis:
  unit: cm^-1
  nu_start: 6000
  nu_end: 6500
  nu_step: 0.01

environment:
  pressure: 1.0
  pressure_unit: atm
  temperature: 296.0
  temperature_unit: K
  path_length: 10.0
  path_length_unit: cm

resident_gases:
  - name: H2O
    concentration: 10000
    concentration_unit: ppm
  - name: CO2
    concentration: 420
    concentration_unit: ppm

variable_gases:
  - name: CH4
    presence: 1
    concentration: 2
    concentration_unit: ppm
  - name: CO
    presence: 0
    concentration: 0
    concentration_unit: ppm

noise:
  gaussian_std: 0.001

baseline:
  type: polynomial
  order: 2
  coefficient_range: [-0.001, 0.001]

output:
  format: hdf5
```

### 7.3 合成结果结构

```text
SpectrumRecord
  sample_id
  wavenumber
  clean_absorbance
  baseline
  noise
  final_absorbance
  transmittance
  resident_concentration_labels
  variable_presence_labels
  variable_concentration_labels
  environment_parameters
  synthesis_parameters
  random_seed
  line_database_version
```

## 8. 桌面 GUI 功能设计

### 8.1 页面结构

| 页面 | 功能 |
| --- | --- |
| 首页/项目面板 | 显示数据库状态、最近任务、快捷入口 |
| 谱线下载 | 批量添加气体与波数范围，启动 HAPI 下载任务 |
| 本地数据库 | 查看已有气体、覆盖范围、谱线数量、完整性状态 |
| 本地检索 | 输入气体和波数范围，预览本地谱线记录和覆盖情况 |
| 单条合成 | 设置一组参数并预览吸收谱 |
| 批量合成 | 设置参数空间、采样策略、输出格式并启动批量任务 |
| 结果浏览 | 查看光谱曲线、标签表、参数表和导出结果 |
| 日志与设置 | 查看任务日志、数据库路径、默认输出路径 |

### 8.2 关键交互规则

1. 合成页面不得自动调用下载功能。
2. 合成前必须检查本地数据库覆盖情况。
3. 覆盖不足时，GUI 显示缺失气体和缺失波数范围，并提供跳转到下载页面的入口。
4. 所有波数输入框固定显示单位 `cm^-1`。
5. 下载任务、合成任务均在后台线程执行，GUI 保持可响应。
6. 光谱预览支持 clean absorbance、baseline、noise、final absorbance 的开关显示。

## 9. 参数校验设计

| 参数 | 校验规则 |
| --- | --- |
| 波数范围 | `nu_start` 与 `nu_end` 不得相等，且必须为正数 |
| 采样间隔 | `nu_step` 必须为正数，点数过大时需要二次确认 |
| 气体名称 | 必须存在于软件内置气体表或用户扩展气体表中 |
| 本地覆盖 | 合成前必须确认气体谱线覆盖完整 |
| 温度 | 必须大于 0 K，建议提示 HITRAN 模型适用范围 |
| 压力 | 必须为正数 |
| 浓度 | 不得为负；变量组分不存在时浓度应为 0 |
| 光程 | 必须为正数 |
| 噪声参数 | 标准差、幅值、尖峰宽度等不得为负 |
| 基线参数 | 多项式阶数、控制点数量等必须在合理范围内 |

## 10. 日志与可追溯设计

### 10.1 下载任务记录

每次下载任务至少记录：

1. 气体名称和 HITRAN 分子编号。
2. 下载波数范围。
3. 实际新增覆盖范围。
4. 下载开始和结束时间。
5. 下载状态、错误信息和重试次数。
6. 写入数据库的谱线数量。

### 10.2 合成任务记录

每次合成任务至少记录：

1. 用户输入配置。
2. 展开后的完整参数表。
3. 使用的本地谱线数据库路径和版本。
4. 每种气体实际检索的波数范围和谱线数量。
5. 软件版本、依赖版本。
6. 随机种子。
7. 每个样本的合成状态。
8. 异常堆栈和失败原因。

## 11. 测试设计

### 11.1 单元测试

| 模块 | 测试重点 |
| --- | --- |
| 波数网格 | 点数、范围、方向、采样间隔正确 |
| 数据库覆盖 | 区间合并、缺失区间检测、覆盖完整性判断 |
| 本地检索 | 按气体和波数范围正确返回谱线 |
| 参数校验 | 非法范围、非法单位、缺失字段能被捕获 |
| 噪声基线 | 随机种子可复现，形状与幅值符合配置 |

### 11.2 物理一致性测试

1. 浓度增加时，同一谱线吸光度应单调增加。
2. 光程增加时，吸光度应近似线性增加。
3. 无噪声、无基线时，相同参数应得到完全一致结果。
4. 混合气体吸光度应等于各单气体吸光度叠加结果。
5. 变量组分不存在时，其存在性标签为 0，浓度标签为 0。

### 11.3 集成测试

1. 在 GUI 中创建 CO2 指定波数范围下载任务，并写入本地数据库。
2. 在本地数据库页面查看 CO2 覆盖范围和谱线数量。
3. 在本地检索页面检索 CO2 指定波数范围谱线。
4. 使用已下载数据生成单条 CO2 吸收谱。
5. 使用 H2O/CO2 与变量组分配置生成批量样本。
6. 导出 HDF5/Parquet 并重新读取，验证光谱数据、标签和元数据一致。

## 12. 性能设计

| 场景 | 策略 |
| --- | --- |
| 大波数范围检索 | 对 `gas_id` 和 `wavenumber` 建索引 |
| 大范围 line-by-line 计算 | 分段计算，避免一次性创建过大数组 |
| 大批量样本 | 参数表分块，批量写入 HDF5/Parquet |
| 重复条件计算 | 对相同气体、温度、压力、波数网格缓存中间吸收截面 |
| GUI 响应 | 下载和合成任务放入后台 worker，主线程只更新状态 |
| 预览模式 | 限制预览点数或使用较粗 `nu_step` |

## 13. 风险与应对策略

| 风险 | 影响 | 应对策略 |
| --- | --- | --- |
| HITRAN/HAPI 访问受网络或授权影响 | 下载失败 | 支持任务重试、失败日志、本地数据库离线使用 |
| 本地数据库覆盖不足 | 合成任务无法执行 | 合成前强制覆盖检查并显示缺失区间 |
| 谱线数据库持续扩展后体积增大 | 检索变慢 | 建立索引、按气体分表或按气体分文件存储 |
| 高分辨率 line-by-line 计算慢 | 批量任务耗时长 | 分块、缓存、并行计算 |
| 噪声和基线模型过于理想 | 训练数据泛化不足 | 后续引入实测背景统计参数和困难样本模板 |
| GUI 长任务阻塞 | 用户体验差 | 使用后台线程/进程和任务队列 |

## 14. 迭代路线

### V0.1 本地数据库原型

- 桌面 GUI 基础框架。
- HITRAN 气体名称与分子编号映射表。
- HAPI 单气体、单波数范围下载。
- 本地谱线数据库 schema。
- 数据库覆盖范围查看与本地检索。

### V0.2 单条合成原型

- 波数网格生成。
- 本地数据库谱线检索。
- 单气体吸收谱合成。
- Voigt 线型。
- clean absorbance 和 transmittance 预览。

### V0.3 混合气体与扰动模型

- H2O/CO2 常驻组分与变量组分混合合成。
- Gaussian 噪声、多项式基线、随机平滑基线。
- final absorbance、baseline、noise 分量输出。

### V0.4 批量数据集生成

- 参数空间展开。
- 随机采样、分层采样、外部参数表导入。
- HDF5/Parquet/NPZ 导出。
- 标签表与元数据导出。
- 批量任务进度、日志和失败样本追踪。

### V1.0 桌面稳定版

- 完整桌面 GUI。
- 本地数据库增量扩展与完整性检查。
- 批量下载任务队列。
- 批量合成任务队列。
- 光谱结果浏览、筛选和导出。
- 完整测试集和用户文档。

## 15. 第一阶段建议实现顺序

1. 确定桌面 GUI 技术栈和工程目录结构。
2. 设计本地气体谱线数据库 schema。
3. 实现 HITRAN 气体名称与分子编号映射。
4. 实现 HAPI 下载服务和下载任务 GUI。
5. 实现数据库覆盖范围管理和本地检索。
6. 实现波数网格、单气体吸收谱合成和预览。
7. 实现混合气体合成、噪声、基线输出。
8. 实现批量合成、标签生成和数据导出。
